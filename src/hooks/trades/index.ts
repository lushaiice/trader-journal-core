/** Trade React hooks (TanStack Query) — domain-driven re-exports. */
export {
  useTradesQuery as useTrades,
  useTradeQuery as useTrade,
  useSaveTrade,
  useDeleteTrade,
} from "@/lib/trades/api";
